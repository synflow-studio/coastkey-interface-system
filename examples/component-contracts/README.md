# Component Contract Examples

These examples are simplified public-safe snippets that show typed props,
slot APIs, variants, states, and token-backed class or CSS-variable patterns.

Files:

- `slot-api-component.example.tsx`
- `product-card-contract.example.tsx`
