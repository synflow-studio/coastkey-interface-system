# Validation Examples

These examples show the public-safe validation story:

- a layer-selected validation matrix;
- source/reference metadata that records truth mode and contract boundary.

Files:

- `validation-matrix.example.md`
- `reference-surface-metadata.example.ts`
