# Runtime Adapter Examples

These examples show how runtime data can be mapped into component props without
letting product components import source data directly.

Files:

- `runtime-adapter-to-props.example.ts`
- `source-backed-view-model.example.ts`
